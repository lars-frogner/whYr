byer.dat, kommuner.dat, sverige.dat  # Files to read place data from
1                                    # Whether to use existing forecast files if present [0 or 1]
1                                    # Whether to keep forecast files after they have been read [0 or 1]
3, 34, 55.9, 72                      # Map extent [min longitude, max longitude, min latitude, max latitude]
i                                    # Map resolution [c ("crude"), l ("low"), i ("intermediate"), h ("high") or f ("full")]
200, 200                             # Interpolation grid resolution [nx, ny]
